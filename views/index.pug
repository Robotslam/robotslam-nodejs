extends layout

block content
  h1= title
  p Welcome to #{title}

  #buttton
    if active
      a.btn.btn-primary(href='/toggle/stop') Stop
    else
      a.btn.btn-primary(href='/toggle/start') Start


